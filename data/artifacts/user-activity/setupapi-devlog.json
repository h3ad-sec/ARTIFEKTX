{
  "id": "setupapi-devlog",
  "name": "SetupAPI Device Install Log (setupapi.dev.log)",
  "category": "file",
  "subcategory": "user-activity/removable-media",
  "os_min": "Windows XP",
  "location": "%SystemRoot%\\inf\\setupapi.dev.log (Windows Vista and later). On Windows XP/2000/Server 2003 the equivalent file is %SystemRoot%\\setupapi.log (note: no 'inf' subfolder, different filename). ANSI plain-text file; when it grows large it is not simply truncated - a related setupapi.upgrade.log is created during OS upgrades to record driver migration for previously-installed devices.",
  "purpose": "System-wide, chronological plain-text log written by the Plug and Play (PnP) manager and SetupAPI recording every device/driver installation operation on the host, including the exact local-time timestamp a device's driver was first installed. For removable media this is the highest-confidence source for establishing precisely when a USB device was FIRST connected to a system, complementing (and independently corroborating) the registry-based USBSTOR 'last connected/removed' property timestamps.",
  "key_fields": [
    "Text log header - operating system version and computer architecture, written once at the top of the file",
    "Text log section header - format '[timestamp process-id.instance description]', e.g. a 'Device Install' section opening with a line like '>>> [Device Install (Hardware initiated) - USB\\VID_xxxx&PID_xxxx\\<serial>]' followed by a 'Section start YYYY/MM/DD HH:MM:SS.sss' line",
    "time_stamp field format: yyyy/mm/dd hh:mm:ss.sss, recorded in the local system time zone at the time of installation (not UTC, unlike most registry timestamps)",
    "Device instance ID string embedded in the section header/body - contains device class, VID (vendor ID), PID (product ID), and the device's reported serial number, present for every Plug-and-Play device, not just USB mass storage",
    "Driver installation outcome lines within the section (e.g., driver package search, selection, and install success/failure), useful for confirming whether a device install actually completed versus was only attempted",
    "Section end marker with final status code, indicating whether SetupAPI considered the installation successful"
  ],
  "normal_baseline": "A single append-only log file growing over the life of the OS install, containing device-install sections for expected hardware (docking stations, printers, and the same recurring set of user storage devices) with first-install timestamps that align with when the user says they first used each device, and no sections for unfamiliar device classes.",
  "anomalies": [
    "A USBSTOR registry entry exists for a device with no corresponding Device Install section in setupapi.dev.log (or vice versa) - the two sources should agree on first-connection time, so a mismatch suggests log tampering, registry manipulation, or that the connection happened under a different/rolled-back log",
    "A Device Install section timestamp inconsistent with the surrounding log entries (out of chronological order), suggesting the log file was edited or entries were spliced in after the fact",
    "The active setupapi.dev.log missing historical entries the analyst expects based on other evidence (e.g., USBSTOR first-install property data older than the earliest entry in the current log), consistent with deliberate deletion/replacement of the log or a Windows Setup/upgrade event that reset it - check for a setupapi.upgrade.log, which can retain migration records for previously-connected devices even after the main log is cleared",
    "A burst of many distinct, unfamiliar VID/PID device installs in a short window, especially immediately before or after a suspected data-exfiltration or intrusion timeframe",
    "A gap in section timestamps spanning a period the host was known to be powered on and in use, suggesting the log was cleared or truncated during that window"
  ],
  "parsing_tools": [
    "Any text editor / findstr/grep (plain ANSI text format, no proprietary parser required)",
    "USB Detective (commercial - parses setupapi.dev.log alongside registry USB artifacts into a unified device timeline)",
    "KAPE (Eric Zimmerman - collects setupapi.dev.log via built-in USB-related collection targets for offline review)"
  ],
  "attack_techniques": [
    "T1091",
    "T1052.001"
  ],
  "anti_forensic_notes": "setupapi.dev.log only records the FIRST installation of a given device/driver combination system-wide - it does not log every subsequent reconnect, so later connect/disconnect timing must still come from registry property timestamps (USBSTOR Properties 0x0066/0x0067) or MountPoints2. Because it is a plain-text, non-protected file under %SystemRoot%\\inf, it is straightforward to delete or edit and is explicitly called out in anti-forensic guidance as a file to remove to obscure USB history; deletion removes the earliest-connection evidence but does not affect the registry-based artifacts, so the two sources should always be checked together. A Windows Feature Update/in-place upgrade can also effectively reset this log, though a setupapi.upgrade.log may retain some historical migration records for devices that existed before the upgrade.",
  "related_artifacts": [
    "usb-device-history"
  ]
}
