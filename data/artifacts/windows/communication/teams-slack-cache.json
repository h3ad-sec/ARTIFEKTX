{
  "id": "teams-slack-cache",
  "name": "Teams/Slack Local Cache (LevelDB)",
  "type": "file",
  "os_min": "Windows 10",
  "location": "Teams 2.x (current, Electron-based): %LOCALAPPDATA%\\Packages\\MSTeams_8wekyb3d8bbwe\\LocalCache\\Microsoft\\MSTeams\\ containing an IndexedDB directory whose LevelDB files hold cached messages/posts/contacts. Legacy classic Teams 1.x: %APPDATA%\\Microsoft\\Teams\\IndexedDB\\. Slack desktop: %APPDATA%\\Slack\\Local Storage\\leveldb\\ for LevelDB key-value data, with a separate Cache_data folder holding cached attachments/media (Chromium disk-cache format). Exact subfolder names can shift between client versions - confirm the live path on the target build before relying on it.",
  "purpose": "Modern Teams and Slack desktop clients are Electron/Chromium apps that persist recent conversation state client-side using LevelDB (Chromium's key-value storage engine, backing IndexedDB and Local Storage) rather than a conventional flat file, so a forensic copy of these directories can yield message text, contact/channel metadata, and cached attachments even without server-side access.",
  "key_fields": [
    "LevelDB directory structure: .ldb (sorted table) files holding the actual key-value records, a MANIFEST file describing the current set of SST files, a CURRENT file pointing to the active manifest, and a LOG/LOCK file - all must be collected together, not just one .ldb file, or the store may not parse",
    "IndexedDB object store records (JSON-like serialized values) containing message text, sender ID, channel/conversation ID, and timestamp for Teams; equivalent Local Storage key-value pairs for Slack workspace/session state",
    "Chromium-format disk cache (Cache / Cache_data folder) holding cached images, files, and other attachment blobs referenced by conversation records, parseable with standard Chrome-cache tooling",
    "Tombstone entries: LevelDB does not overwrite deleted records in place - a delete is appended as a new entry with an empty value and a higher sequence number, so the original key (and sometimes prior value fragments) can remain recoverable in the .ldb file until the database is compacted",
    "Workspace/tenant and user-account identifiers embedded in the store path or records, useful for confirming which of several Teams/Slack identities the cache belongs to on a shared machine"
  ],
  "normal_baseline": "A single active LevelDB store per installed client tied to the logged-on user's profile, growing incrementally with normal conversation use, whose message timestamps align with the user's known working hours and known chat/channel memberships.",
  "anomalies": [
    "Presence of a Teams/Slack LevelDB cache directory persisting after the application was uninstalled - deletion of the app does not remove the cache, so recovered messages can predate or postdate the visible install by longer than expected",
    "Recovered tombstone/deleted-record remnants in .ldb files containing message content the user claims was never sent or was deleted, indicating deleted messages are still forensically recoverable pre-compaction",
    "Cache data belonging to a workspace/tenant or account the user is not authorized to access on that host, suggesting credential reuse or unauthorized account access",
    "Message timestamps in the cache with no corresponding entry in server-side/admin-exportable chat logs (where cross-checkable), suggesting local tampering or a client-side spoofing tool rather than genuine app-generated data",
    "Unusually large or unexplained growth in Cache_data volume shortly before a suspected exfiltration window, consistent with bulk attachment download and caching"
  ],
  "parsing_tools": [
    "forensicsim (open-source Autopsy module/parser for Microsoft Teams IndexedDB LevelDB data)",
    "leveldb-dump / ccl_chrome_indexeddb (open-source LevelDB and Chromium IndexedDB record parsers)",
    "ChromeCacheViewer (Nirsoft, for the Chromium-format Cache_data attachment cache)"
  ],
  "attack_techniques": [
    "T1213.005"
  ],
  "anti_forensic_notes": "LevelDB's append-only, log-structured design means deleted messages are not immediately purged - they persist as recoverable tombstones in .ldb files until the database runs compaction, so a user 'deleting' a message in the Teams/Slack UI does not reliably remove it from the local cache at the time of collection. Conversely, waiting too long before imaging a system, or the client running a compaction pass, can permanently remove that recoverable content - LevelDB evidence is time-sensitive in a way flat-file artifacts are not. Uninstalling the client does not delete the AppData cache directory, so absence of the parent application does not mean absence of message history.",
  "interpretation": "",
  "related_artifacts": [
    "chrome-edge-browser-artifacts"
  ]
}
